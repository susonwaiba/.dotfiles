return {
    -- {
    --     'rest-nvim/rest.nvim',
    --     dependencies = { 'nvim-lua/plenary.nvim' },
    --     config = function()
    --         vim.keymap.set("n", "<leader>rr", "<cmd>lua require('rest-nvim').run()<CR>", { desc = "Run rest" })
    --         require("rest-nvim").setup({
    --             --- Get the same options from Packer setup
    --         })
    --     end
    -- },
}
